# Local-only: organizer scorer + answer key. Never commit.
_local/
ground_truth.json

# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Node
node_modules/
.next/
dist/
build/

# Outputs
submission*.json
!data/sample_submission.json
*.log

# OS / editors
.DS_Store
.vscode/
.idea/
.obsidian/

# Runtime state (local store) — cloud deploys use Supabase instead
data/reviews.json
data/processed_extra.json
data/uploads/
.claude/

# stray nested clone (contains its own .env — never commit)
AverisHackathon/

# LLM response cache (regenerated on demand)
data/llm_cache/
